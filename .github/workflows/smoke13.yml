name: smoke13

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]
    steps:
      - run: echo ok
      - if: matrix.python-version == '3.11'
        run: echo "implicit-single-quote ok"
