name: smoke14

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]
    steps:
      - run: echo ok
      - if: runner.os == 'Linux'
        run: echo "non-matrix-if ok"
